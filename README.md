# 🌙☀️ MagoNews — Modo Dark & Modo Light

Este projeto é uma página HTML simples que demonstra a alternância entre **Modo Escuro (Dark Mode)** e **Modo Claro (Light Mode)** utilizando **JavaScript puro**, sem bibliotecas externas.

---

## 📄 Descrição

A página possui:
- Um título (`MagoNews`)
- Um texto informativo
- Dois botões:
  - **Modo Dark** → fundo preto e texto branco
  - **Modo Light** → fundo branco e texto preto

A troca de temas acontece dinamicamente através da manipulação do `style` do `body`.

---

## 🛠️ Tecnologias Utilizadas

- **HTML5**
- **JavaScript (Vanilla JS)**

---

## 📂 Estrutura do Código

- `HTML`  
  Define a estrutura da página (título, parágrafo e botões).

- `JavaScript`  
  Contém duas funções:
  - `modoDark()` → ativa o modo escuro
  - `modoLight()` → ativa o modo claro

---

## ▶️ Como Executar

1. Copie o código HTML.
2. Salve o arquivo como:
